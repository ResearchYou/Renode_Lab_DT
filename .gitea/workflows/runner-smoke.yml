name: runner-smoke

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  smoke:
    runs-on: debian-latest
    steps:
      - name: Smoke step
        run: |
          echo "runner smoke start"
          uname -a
          echo "runner smoke ok"
