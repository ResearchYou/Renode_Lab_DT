name: docker-compose-build

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

defaults:
  run:
    shell: sh

jobs:
  compose-build:
    runs-on: debian-latest
    steps:
      - name: Resolve compose directory
        run: |
          set -eu
          COMPOSE_FILE="$(find . -maxdepth 5 -type f -name docker-compose.yml | head -n1 || true)"
          if [ -z "$COMPOSE_FILE" ]; then
            echo "docker-compose.yml not found in runner workspace."
            exit 1
          fi
          COMPOSE_DIR="$(dirname "$COMPOSE_FILE")"
          echo "$COMPOSE_DIR" > .compose-dir
          echo "Using compose directory: $COMPOSE_DIR"
          ls -la "$COMPOSE_DIR"

      - name: Ensure Docker + Compose availability
        run: |
          set -eu

          if ! command -v docker >/dev/null 2>&1; then
            echo "Docker is not available on this runner."
            exit 1
          fi

          if ! docker compose version >/dev/null 2>&1; then
            echo "Docker Compose plugin is not available on this runner."
            exit 1
          fi

          docker version
          docker compose version

      - name: Build all compose services
        run: |
          set -eu
          COMPOSE_DIR="$(cat .compose-dir)"
          cd "$COMPOSE_DIR"
          docker compose build
